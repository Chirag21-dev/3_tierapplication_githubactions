apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rating-app.fullname" . }}-test-connection"
  labels:
    {{- include "rating-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rating-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
