apiVersion: v1
kind: Service
metadata:
  name: {{ include "rating-app.fullname" . }}
  labels:
    {{- include "rating-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rating-app.selectorLabels" . | nindent 4 }}
